package org.example.util.filter;

public class FilterFieldValidator {

    public static void validateField(String field, Class<?> entityClass) {
//        Set<String> validFields = new HashSet<>();
//
//        for (Field declaredField : entityClass.getDeclaredFields()) {
//            validFields.add(declaredField.getName());
//
//            Column column = declaredField.getAnnotation(Column.class);
//            if (column != null && !column.name().isEmpty()) {
//                validFields.add(column.name());
//            }
//        }
//
//        if (!validFields.contains(field)) {
//            throw new InvalidFilterFieldException(field);
//        }
    }

}

